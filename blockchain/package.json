{
  "name": "aryan-certificate-blockchain",
  "version": "1.0.0",
  "description": "Smart contracts for Aryan Certificate Portal",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:basesepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "verify:amoy": "hardhat verify --network amoy",
    "verify:basesepolia": "hardhat verify --network baseSepolia",
    "grant-role": "node scripts/grant-role.js",
    "check-role": "node scripts/check-role.js",
    "stats": "node scripts/get-stats.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.3",
    "hardhat": "^2.19.4"
  }
}
